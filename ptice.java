
import java.util.Scanner;

public class ptice {

    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        int aCount = 0;
        int bCount = 0;
        int gCount = 0;

        // check answers
        for (int i = 0; i < scanner.nextInt(); i++){
            checkAns(i, scanner.next(), aCount, bCount, gCount);
        }

        // find highest and print

    }

    public static void checkAns(int i, String answer, int aCount, int bCount, int gCount){

    }
}
